name: Deploy to Staging

on:
  push:
    branches: [staging]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build frontend
        run: |
          cd frontend
          npm install
          npm run build

      - name: Upload to S3
        run: aws s3 sync ./frontend/build s3://staging-bucket

      - name: Deploy backend
        run: |
          ssh ubuntu@ec2 << 'EOF'
          git pull origin staging
          python manage.py migrate
          sudo systemctl restart gunicorn
          sudo systemctl restart celery
          EOF
